<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.zh.mylangchain4jtools.mapper.LostItemMapper">
    <resultMap id="BaseResultMap" type="io.github.zh.mylangchain4jtools.entity.LostItem">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="item_name" property="itemName" />
        <result column="item_description" property="itemDescription" />
        <result column="category" property="category" />
        <result column="lost_location" property="lostLocation" />
        <result column="lost_time" property="lostTime" />
        <result column="contact_info" property="contactInfo" />
        <result column="contact_name" property="contactName" />
        <result column="remarks" property="remarks" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <insert id="insert" parameterType="io.github.zh.mylangchain4jtools.entity.LostItem" useGeneratedKeys="true" keyProperty="id">
        insert into lost_items (user_id, item_name, item_description, category, lost_location, lost_time, contact_info, contact_name, remarks, status, create_time, update_time)
        values (#{userId}, #{itemName}, #{itemDescription}, #{category}, #{lostLocation}, #{lostTime}, #{contactInfo}, #{contactName}, #{remarks}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectById" resultType="io.github.zh.mylangchain4jtools.entity.LostItem">
        select * from lost_items where id = #{id}
    </select>

    <select id="selectByUserId" resultType="io.github.zh.mylangchain4jtools.entity.LostItem">
        select * from lost_items where user_id = #{userId}
    </select>

    <select id="searchByKeyword" resultType="io.github.zh.mylangchain4jtools.entity.LostItem">
        select * from lost_items 
        <where>
            <if test="keyword != null and keyword != ''">
                and (item_name like CONCAT('%', #{keyword}, '%') or item_description like CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                and category = #{category}
            </if>
            <if test="location != null and location != ''">
                and lost_location = #{location}
            </if>
        </where>
    </select>

    <update id="updateStatus">
        update lost_items set status = #{status} where id = #{id}
    </update>

    <update id="update">
        update lost_items
        <set>
            <if test="itemName != null">item_name = #{itemName},</if>
            <if test="itemDescription != null">item_description = #{itemDescription},</if>
            <if test="category != null">category = #{category},</if>
            <if test="lostLocation != null">lost_location = #{lostLocation},</if>
            <if test="lostTime != null">lost_time = #{lostTime},</if>
            <if test="contactInfo != null">contact_info = #{contactInfo},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="remarks != null">remarks = #{remarks},</if>
            <if test="status != null">status = #{status},</if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>
</mapper>